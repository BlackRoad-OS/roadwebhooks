openapi: 3.0.3
info:
  title: Webhooks API
  description: Webhooks microservice - Part of BlackRoad OS
  version: 1.0.0
  contact:
    url: https://support.blackroad.io
servers:
  - url: https://api.blackroad.io/roadwebhooks
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
                  timestamp: { type: string }
